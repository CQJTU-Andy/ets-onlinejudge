/**
 * 基础配置
 * @type {{resUrl: string, apiContext: string}}
 */
var $config = {
    apiContext: 'http:/localhost:8080/',
    // apiBoardContext: 'http:/localhost:8080/',
    // apiBoardContext: 'http:/localhost:8080/',
    resUrl: 'http:/localhost:8080/' // 前端资源访问根目录
};

